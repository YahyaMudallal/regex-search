# SVG générés par Matplotlib : conserver les octets et leurs empreintes, y compris
# les espaces de fin de ligne dans les coordonnées des chemins SVG.
docs/assets/*.svg linguist-generated=true -whitespace
